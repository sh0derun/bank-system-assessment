{
  "name": "bank-system-assessment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "dotenv -- nodemon src/server.js",
    "start": "dotenv -- node src/server.js",
    "db:migrate": "dotenv -- npx prisma migrate dev",
    "db:reset": "dotenv -- npx prisma migrate reset",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Razik Anass",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.5.0",
    "dotenv-cli": "^6.0.0",
    "express": "^4.18.2",
    "express-validator": "^6.14.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "prisma": "^4.5.0"
  }
}
